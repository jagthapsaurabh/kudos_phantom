fastapi
uvicorn[standard]
pandas
numpy
requests
scikit-learn
pydantic
python-multipart
passlib[bcrypt]
python-dotenv
sqlalchemy
bcrypt
# PostgreSQL driver — required when DATABASE_URL points at a Postgres instance
# (e.g. postgresql://user:pass@host:5432/dbname). Use psycopg2-binary for a
# precompiled wheel; psycopg2 for a source build.
psycopg2-binary
